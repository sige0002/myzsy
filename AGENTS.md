# myzsy

WezTerm / WSL / zsh / tmux の設定テンプレートと導入スクリプト。

- 用途と手順は [README.md](README.md)、テンプレートは `config/`、導入処理は `scripts/`。このリポジトリのファイルは各PCの実設定バックアップではない。
- 依頼がテンプレート修正ならリポジトリ内で完了させる。ユーザー環境へ適用する依頼がある場合だけ、対象の実設定と差分を確認して反映する。
- シェルスクリプトの構文確認は shebang に対応する `bash -n` / `zsh -n` を使う。確認のためにインストーラーを実行しない。
- シェル変数に `HOME` / `home` / `CODEX_HOME` を流用しない。ダウンロードの検証やアーキテクチャ判定を維持する。
- キーバインド、導入方法、設定の置き場所が変わったら対応する README の節を更新する。現在稼働中の tmux セッションを検証目的で終了しない。
